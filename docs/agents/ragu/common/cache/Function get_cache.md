# Function get_cache (defined in ragu/common/cache.py at lines 347-353)

def get_cache(dir: str | pathlib.Path) -> collections.abc.MutableMapping[str, typing.Any]:
    """Get or create a key-value cache which uses the specified directory."""
    ...